# Patches Directory

This directory can hold optional standalone diff patches or architecture-specific patches for Xiaomi MiMo.

The primary patching mechanism is driven automatically by `scripts/patch.js`.
